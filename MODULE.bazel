"""
HBF - Web Compute Environment
Bazel module configuration (bzlmod)
"""

module(
    name = "hbf",
    version = "0.1.0",
)

# Bazel rules for C/C++ compilation
bazel_dep(name = "rules_cc", version = "0.1.1")

# Platform support
bazel_dep(name = "platforms", version = "0.0.11")

# TODO: Add musl toolchain configuration
# For Phase 0, we'll use the default system toolchain
# In later phases, we'll configure a proper musl-based toolchain
# that produces 100% static binaries
