# HTTP server and routing (Phase 1+)

cc_library(
    name = "server",
    srcs = ["server.c"],
    hdrs = ["server.h"],
    deps = [
        "//internal/core:config",
        "//internal/core:log",
        "@civetweb//:civetweb",
    ],
    visibility = ["//visibility:public"],
)
